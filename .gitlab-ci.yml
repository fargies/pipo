image: "node:alpine"

stages:
  - build
  - test
  - pages

build:
  stage: build
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/

test:
  stage: test
  script:
    - npm run test
  dependencies:
    - build

pages:
  stage: pages
  script:
    - npm run jsdoc
    - mv docs public
  artifacts:
    paths:
      - public
  only:
    - master
